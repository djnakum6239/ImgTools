[package]
name = "imgtools"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
lzma-rust2 = { version = "0.21.0", default-features = false, features = ["std", "xz", "encoder"] }
bzip2-rs = "0.1.2"
jni = "0.21"
